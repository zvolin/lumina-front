// Imports
// ------------
import styled, { css } from 'styled-components';
import { bp } from '@tackl';

// Exports
// ------------
export const Jacket = styled.input(
    props => css`
        position: relative;
        width: 100%;
        padding: 1.2rem;
    `
);
